---
const { name, description, dates } = Astro.props;
---
<div class="p-8 sm:p-10 lg:flex-auto">
    <h3 class="text-4xl font-bold tracking-tight text-gray-900">{name}</h3>
    <p class="mt-6 text-base leading-7 text-gray-600">{dates}</p>
    <p class="mt-6 text-base leading-7 text-gray-600">{description}</p>
</div>